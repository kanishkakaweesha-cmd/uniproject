# Example environment variables for the delivery app
# Copy to .env and edit values for your environment
#
# For a local MongoDB (default):
# MONGO_URI=mongodb://localhost:27017/delivery_db
#
# For MongoDB Atlas (replace <username>, <password>, <myCluster>, <dbname>):
# MONGO_URI=mongodb+srv://<username>:<password>@<myCluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
# See instructions below for creating a free Atlas cluster and getting the connection string.

MONGO_URI=mongodb://localhost:27017/delivery_db
# Optional: API key to allow ESP32 devices to POST without logging in
ESP32_API_KEY=
# Optional: default company name stamped on ESP32-created packages
ESP32_COMPANY_NAME=
